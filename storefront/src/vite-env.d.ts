/** The storefront's own public URL, fixed at build time. See vite.config.ts. */
declare const __SITE_URL__: string

/** The seller's npub, encoded at build time from SELLER_PUBKEY. */
declare const __SELLER_NPUB__: string
