# Known defects

Findings from the slice 0–5 review that were **verified to still reproduce** and then
deliberately not fixed. This is the deferral ledger: everything here is a real behaviour of the
code on `main` today, with the reason it was left and the shape of the fix.

Line numbers were re-verified against the tree on **2026-08-20**, after the five review fixes
landed. `/docs/spike-findings.md` still wins on any protocol detail.

**Slice 6 (2026-08-21) closed one entry and part of a second, and added two of its own.** Closed
items are struck through and kept, because the reasoning is worth more than the row. Most severe
first.

**Milestone A (2026-08-24) closed the four confirmed rows in "Added by the review panel" and
emptied the seven-claim list under them.** Every one of the seven is now either a closed entry or
an open row with its reasoning — that section is a scoreboard rather than a queue, and it should
stay empty. What milestone A opened is in "Added by milestone A" at the foot. Two of those rows
are holes its own fixes left, written down deliberately: a guard that reads as a guarantee and is
not one is worse than no guard.

| what breaks | where | what it costs a user | why it is deferred | what fixing it looks like |
|---|---|---|---|---|
| ~~**Offer minting has no idempotency key.**~~ **CLOSED 2026-08-21 by slice 7's Phase 0**, exactly as the fix column prescribed. `builder/src/manage.ts` gained `listOffers` (Manage `list`, `{resource:'offer',action:'list',pointer}`) and `mintOffer`, which dedupes on `label === listingD(slug)` before creating and re-derives the price from the pointer's own TLV 4 rather than the node's echo. `publish.ts` calls `mintOffer`; `createOffer` is still exported for `check-manage.ts`, which is testing the create path deliberately. One decision worth recording: a FAILED `list` mints rather than reusing — an unreachable node must not read as "you have no offers", because a duplicate inert offer is recoverable and an item published with no payable offer is a sale that cannot happen. Proved against the live node by `check-manage.ts`'s seven new checks: a retry reuses the offer it already minted, and a price change still mints a new one. Original text: **Offer minting has no idempotency key.** `publish()` calls `createOffer` unconditionally, before anything is signed. Every failure after that point — a declined signature, a bunker timeout, a relay refusal — throws, and `main.ts`'s `finally` re-enables the Publish button. The seller presses it again and a **second payable offer** is minted for the same item. |
| ~~**A builder item with `stock 1` is still invisible to the watcher.**~~ **CLOSED 2026-08-21 by slice 6, exactly as prescribed.** The ladder file now carries the item's `noffer` on itself — `{ [d]: { units, noffer, steps } }` — written by `builder/src/publish.ts` and `spike/seed-listings.ts`, resolved by the new `spike/ladder.ts` `nofferOf` in descending authority: the file, then a rung tag, then `.offers.json`. Additive, so no existing ladder file has to be re-cut to keep working, and `.offers.json` is now a pure fixture artefact. Tested in `ladder.test.ts` ("a one-of-a-kind item is watchable"), which asserts the stock-0 rung advertises nothing and that the old inference returns `undefined` on it. The collision this row deferred against — slice 6's edit flow re-cutting ladders — was the reason to do it here rather than the reason to wait. Original text: **A builder item with `stock 1` is still invisible to the watcher.** The 2026-08-20 fix reads the offer out of the rung's own `clink_offer` tag, which closed this for every multi-unit item. A one-unit item has exactly one rung — the stock-0 one — and `ladder.ts:50` `atStock` strips `clink_offer` at stock 0 by design. The fallback is `.offers.json`, which only `mint-offers.ts` writes and only for the fixture's items. | `spike/watch-sales.ts:80-93`, against `spike/ladder.ts:41-50` | The watcher logs `no decodable offer in its ladder or …/.offers.json — not watching` and skips the item. It sells, and the storefront keeps advertising it as available with a live Buy button — the oversell that slice 7 does not yet exist to refund. Most yard-sale items are one-of-a-kind, so this is the common case, not the edge one. | Fixing it means changing the shape of the ladder file, which `watch-sales.ts` reads and `publish.ts` writes — outside a five-fix follow-up, and it collides with slice 6's edit flow, which has to re-cut ladders anyway. | Carry the noffer on the ladder file itself rather than inferring it from a rung: `{ [d]: { units, noffer, steps } }`, written by `builder/src/publish.ts:148` and read first by `nofferOf`. Two lines each side, one file-format bump, and it makes `.offers.json` a pure fixture artefact. |
| **The NIP-46 client key is described as if it were disposable, and it is a bearer credential.** The comment calls it "not the seller's identity, it holds no funds, and burning it costs one re-approval". All three are true and together they are misleading: a NIP-46 grant is keyed to *this* pubkey, it is granted against the eight `sign_event` kinds in `PERMS`, and Amber stores a remembered grant with `acceptUntil = Long.MAX_VALUE`. It also survives Disconnect by design — `forgetBunker()` removes the bunker URI and keeps the key (`signer.ts:74-76`). | `builder/src/signer.ts:57-72` | Anyone who reads this localStorage value — a shared laptop, an XSS on the builder's own origin, a synced browser profile — can sign kind 30402 listings as the seller and mint kind 21003 offers on the seller's node, with **no further prompt on the phone**. The seller's own honest mental model of "the key is on my phone" stops being the whole story. | No code change was in scope, and there is no obviously better place to put it: a fresh key per session is the thing the comment correctly argues against. | Rewrite the comment to say what it is: a bearer credential to a standing signing grant, which survives Disconnect, and whose revocation is on the bunker side (Amber's app list), not here. Optionally have Disconnect offer to burn it. No behaviour change. |
| ~~**Testnet, regtest and signet invoices are accepted.**~~ **CLOSED 2026-08-21 by slice 7's Phase 0.** `storefront/src/offer.ts:121` is now `^lnbc` and `offer.test.ts` asserts `lntb`, `lnbcrt` and `lnsb` all read as `null`. The old test asserting "testnet, same rules" is inverted and says why. The reason it moved from cosmetic to real: slice 7's refund path *pays* a BOLT11 that arrived from a stranger's wallet via an LNURL host or an noffer, so the chain is now our question rather than the payer's wallet's. |
| ~~**The one key in the project outside a Signer has no notice naming it.**~~ **CLOSED 2026-08-21 by slice 7's Phase 0.** `storefront/src/buy.ts` now carries a `KEY HANDLING NOTICE` giving the three reasons the ephemeral per-purchase key is a rule-2 exception rather than a violation: it is neither party's identity, CLINK requires the payer to sign and the receipt is encrypted to whoever did, and a fresh key per purchase is the privacy posture rather than an accident. `grep -rn 'KEY HANDLING'` now returns shipped browser code alongside the spike scripts. |
| ~~**Nothing reads `imeta`.**~~ **PARTLY CLOSED 2026-08-21.** `builder/src/admin.ts` `imetaValues` reads it, and the reader earns the tag rather than excusing it: an edit is a re-publish (there is no update verb in NIP-01), so `fallback` has to be read back or pressing Save silently takes a four-server mirror down to one, and `alt` has to be read back or the accessibility text is lost. `x` and `dim` are still written and unread — they are for a storefront that hash-checks a fetch, which is still not on any slice. Original text: **Nothing reads `imeta`.** `builder/src/listing.ts:44-82` justifies each field by what the storefront will do with it — `x` "a content address the storefront can check a fetch against", `fallback` "in case `url` fails". `grep imeta storefront/src` returns one CSS comment (`style.css:141`) and no code. | written at `builder/src/listing.ts:72-82`, unread anywhere in `storefront/src` | Nothing today. It costs a reader again: the tag reads as load-bearing and is inert, and the `fallback` list — which the 2026-08-20 fix just made accurate — buys nothing until somebody consumes it. | Consuming it is real storefront work (hash-check a fetched blob, retry a failed image against `fallback`) with its own byte budget, and it is not on any slice. | Either add the `fallback` retry to the storefront's `srcset` path, or change the comment from "what the storefront will do" to "what a client could do" and say the tag is written for other clients. |
| ~~**The two functions that decide how much stock is left cannot be tested.**~~ **PARTLY CLOSED 2026-08-21**, by doing what the fix column says for the two functions slice 6 needed: `isStale` and `nofferOf` were written into `spike/ladder.ts` — the already-tested module — rather than inline in the script, and both have tests. `settledCount`, `stepFor`, `imagesFor` and `tagsFor` are unchanged and still untestable. Original text: **The two functions that decide how much stock is left cannot be tested.** `settledCount` (how many units sold) and `stepFor` (which pre-signed rung to publish) are the whole money-facing half of the watcher, and the file has top-level effects — three `existsSync` throws, a `connectPub`, an `await tick()` and a `setInterval` — so importing it runs it. Same for `imagesFor` and `tagsFor` in the seeder. | `spike/watch-sales.ts:157`, `:141`, effects at `:62-64`, `:111`, `:217`, `:224`; `spike/seed-listings.ts:190`, `:203` | Nothing directly — `ladder.test.ts` covers `targetStock`, and the 2026-08-21 real-money run exercised `settledCount` end to end. It costs future confidence: a change to either is unverifiable without spending sats. | Splitting a script into a module plus an entry point is a refactor across three files, and both scripts are demo infrastructure rather than shipped product. | Move the pure functions into `spike/ladder.ts` (which is already the tested module) and import them back. No behaviour change. |
| ~~**The cheapest real test gap is `decodeNmanage`, and it has none.**~~ **CLOSED for `decodeNmanage` 2026-08-21 by slice 7's Phase 0; `render.ts` is untouched and still 402 untested lines.** New `builder/src/manage.test.ts`, 9 tests, mirroring `offer.test.ts`: corrupt checksum, wrong prefix (an noffer and an ndebit with identical first TLVs are both refused), truncated TLV, missing pointer, non-`wss://` relay, repeated-TLV override, whitespace. Writing them found a real defect: the `nmanage1` prefix was tested on the UNTRIMMED string while the trim happened inside `bech32.decode`, so a paste with a trailing newline worked and one with a leading space returned the same `null` as a corrupt pointer — and the UI cannot tell a seller which of those happened. Fixed by trimming first. The TLV loop itself was about to be copied a third time for `decodeNdebit`, so `parseTLV`/`tlvText` are now exported from `storefront/src/offer.ts` and the three pointers share one parser. ~~`render.ts` still needs a DOM harness, which is still a decision rather than a follow-up fix.~~ **CLOSED 2026-08-21 by slice 8's Phase 0, and the decision was "no harness".** The measurement that settled it: `render.ts` touches `document` only inside function bodies, so `await import('./render.ts')` already succeeds in bare node — `node -e "import('./src/render.ts').then(m => console.log(Object.keys(m)))"` prints the exports and installs no globals. The decisions in that file were untestable because they were **private**, not because they needed a browser. Four names were exported and `storefront/src/render.test.ts` now holds 11 tests over `declineText` (all five Offers codes plus code 0 and an unknown code), `formatPrice`, `stockNote` and `sats`. Zero new dependencies, same `node --test` style as the other six suites. Writing them found one thing worth keeping: `Intl.NumberFormat` accepts **any** three ASCII letters and invents a formatting (`ZZZ 5`, `XBT 5`), throwing only on a length that is not three — so `formatPrice`'s try/catch fires on `btcs`, not on the plausible-looking codes it reads as if it guards. Both branches are now asserted. What stays untested is the **markup**: `renderIndex`, `renderDetail` and `renderBuy` build DOM and nothing here asserts what they build. That is the honest boundary, it needs jsdom or a browser, and the browser is a session that already exists (`/docs/prompts/browser-verify-and-deploy.md`). |
| what breaks | where | what it costs a user | why it is deferred | what fixing it looks like |
|---|---|---|---|---|
| **An edit cannot remove a photo, only replace every photo at once.** The form has one file input and it writes all three renditions; there is no way to publish an item that had a photo and no longer should. Choosing a new file replaces the set, which is the common intent, but "this photo is wrong and I have no better one" has no path. | `builder/index.html` `#photo`, `builder/src/main.ts` `onPhoto` / `editItem` | A seller stuck with a bad photo until they take a better one. Nothing is lost and nothing is wrong on the wire — the listing is exactly what it was. | It is a UI affordance, not a correctness problem, and the honest fix is a photo *list* with per-rendition removal, which is a redesign of the upload block rather than a button. | A "remove the photo" button next to `#photo-state` that sets `photos = []` and `servers = []` and calls `refreshCost()`. Three lines, and it needs one line of copy explaining that a listing with no photo is legal. |
| **The stale-ladder check runs once, at watcher startup.** Edit an item while `watch-sales.ts` is running and it keeps holding the superseded rungs for the rest of the session, publishing no-ops the relay accepts and drops (findings §13.26). The builder's copy says to restart the watcher and `check-admin.ts` reports it, but nothing stops it happening. | `spike/watch-sales.ts`, the block above `settledCount` | The exact oversell the check exists to prevent, in the window between an edit and a restart. Bounded by the seller doing what the publish screen tells them, which is not a guarantee. | Re-checking every tick means a relay read per poll per item — five items at 5s is 60 relay reads a minute for a state that changes when a human presses a button. A watcher that re-read its own ladder file on change would be the better shape and is a bigger change than this slice. | Watch `.ladder.json` with `fs.watch` and re-derive `watching` when it changes — that is the event that actually matters, it fires exactly when the seller drops the new file in, and it re-runs the same `isStale` check for free. |
| **"k of N sold" is per-browser, because `units` only exists in localStorage.** The panel derives units sold as `units − stock`, and `units` comes from the ladder this browser cut. Open the panel on a second laptop, or after clearing site data, and every item reads `(no ladder here)`. | `builder/src/main.ts` `metaLine`, against the `lamppost.ladder.<pubkey>` entry | A seller who authored on one machine and opened the panel on another sees no sold counts at all. The listings, the offers and the money are all fine; only the *denominator* is missing. | Nothing on a relay records what an item's stock started at, and the number that would fix it — the node's settled-invoice count — is the one a browser behind a Signer cannot read (findings §13.25). The alternatives are a public tag naming the original stock, which leaks how much a seller expected to sell, or a NIP-78 event, which is a second encrypted document to keep in sync. | Fold `units` into the existing NIP-78 shop document (`builder/src/notes.ts`), which is already encrypted to self, already one event, and already loaded by the panel. It would then follow the seller between machines like the notes do. |

---

## Added by slice 7 (2026-08-21)

Same rules: verified to reproduce, deliberately not fixed, with the reason and the shape of the
fix. **Unlike every previous batch, two of these are on the money path** — that is what it costs
to have a path that writes rather than reads.

| what breaks | where | what it costs a user | why it is deferred | what fixing it looks like |
|---|---|---|---|---|
| **Losing `spike/.refunds.json` can pay a refund twice.** It is the only durable record of which oversells have been paid, because the node has none — there is no "already refunded" field on an invoice, and CLINK's `k1` turned out to be in-memory with a 5-minute TTL (findings §13.28). Delete the file, or run the watcher on a second machine, and every oversell it has already refunded is recomputed from the node as still owed. | `spike/refund.ts` (the journal), `spike/watch-sales.ts` `refundOversells` | A buyer paid twice for one oversell. Bounded by the node's frequency cap — the damage per interval cannot exceed it — but not prevented. | The alternatives are all worse or unbuildable today. The node cannot hold it. A relay could, but the watcher holds no signing key by design (spec §7.2), so it cannot publish a record of what it did. An encrypted NIP-78 document has the same problem. The honest answer is a local file, and the honest disclosure is this row. | Either (a) note the refund's payment hash back onto something the node *does* store — no field exists today, so this is an upstream ask; or (b) reconcile against the node's outgoing payments at startup, matching on amount and destination, which is a heuristic rather than a key. (b) is buildable now and is worth doing before this is used by anyone but its author. |
| **PARTLY CLOSED 2026-08-21 by slice 8's Phase 0** — the reporting half is built, exactly as the fix column prescribed; the deciding half is still deliberately absent. `summarise()` now reads the node's outgoing payments (`GetUserOperations`) and prints those matching a `pending` row's amount inside a ±15-minute window, with `operationId` and timestamp. `matchingPayments` is a pure function in `spike/refund.ts` with 5 tests. **It is labelled a heuristic everywhere it prints and nothing branches on it**: the node stores no link between a debit and the settled invoice that caused it, so two refunds of the same amount in the window are indistinguishable and both are shown rather than one being picked. Two shapes cost a round trip each and are now recorded: the request needs **all six cursors** present (`paymentManager.ts:1130-1135` dereferences every one with no default), and the response key is the node's own typo `latestOutgoingUserToUserPayemnts` (`structs.proto:657`). Original text: **A `pending` refund is never resolved by the software, only reported.** If the node never answers a debit, the journal row stays `pending` forever: the watcher reprints it every five minutes and will not retry it, because retrying might double-pay and dropping it might strand a buyer. | `spike/watch-sales.ts` `refundOversells`, the `code === 0` branch | A refund that may or may not have been sent, and a person has to look at the node's outgoing payments to find out which. On a demo day that is a line in a terminal nobody is reading. | Deciding automatically means guessing about money, in a case where both guesses are wrong in a different direction. Refusing to guess is correct; making the human's job easy is a feature that was not in this slice. | Print the node's matching outgoing payments next to the pending row — `GetUserOperations` filtered by amount and time window — so the reconciliation is one glance rather than an investigation. Or a `--reconcile` mode that does it and asks. |
| ~~**The LNURL half of the refund path has never resolved a real address.**~~ **CLOSED 2026-08-24 by the milestone A review — and closing it cost one free call, after three slices of recording it as unproven.** `resolvePointer('…@coinos.io', 1000)` returned a BOLT11 for exactly 1,000 sats in **1,540 ms**: both hops, DNS vetted, `isPrivateAddress` passing a real public host, TLS validated against the hostname while connected to the vetted IP, a real LUD-06 `payRequest` parsed, `minSendable`/`maxSendable` accepting the amount, the callback returning a `pr`, and `invoiceSats` matching exactly — a mismatch would have refused. **Resolving is free; it returns an invoice nobody pays.** The row's own fix column said this needed "a real settled sale" and "a wallet and a person" — it needed neither, which is why it sat open for three slices. What it did NOT need was the demo beat, and that conflation is the lesson worth keeping. It also immediately found item 27: the first address tried was a BIP-353 one and could never have worked. Original text: **The LNURL half of the refund path has never resolved a real address.** *(Slice 8's Phase 0 removed what was blocking it: `check-buy.ts` hardcoded `check-buy@example.com` at `:59` and `:72`, so every invoice it settled was unrefundable by construction. It now takes `--pointer <address-or-noffer>`, validates it with the same `isPointer` the buy form uses, and **`--pay` refuses to run without it** — a settled invoice stores that value forever and the node cannot correct one later.)* All three settled invoices on the live node carry `@example.com` placeholders left by `check-buy.ts`, so `resolvePointer`'s LNURL branch is covered by unit tests on `lnurlpUrl` and by nothing on the wire. The noffer branch is proven, because it is `storefront/src/buy.ts` unmodified and that module has been paid with real money. | `spike/refund.ts` `resolvePointer`, the LNURL branch | A refund to a Lightning address could fail on a shape the LUD-06 validator gets wrong, and the failure mode is a `queued` row rather than a wrong payment — so it costs a delay and a manual handover, not money. | It needs a real settled sale carrying a real Lightning address, which needs a wallet and a person. The unit tests cover the URL construction and the response validation; what is unproven is one real host's JSON. | Buy `mugs` for 1,000 sats supplying a real Lightning address as the refund pointer, then engineer the oversell. That run also proves the whole slice end to end and is the demo beat — see `/docs/status.md`. |
| **`payDebit`'s success path has never returned `ok`.** Every debit driven so far was one the node refused — over the cap, or banned, or a k1 replay. That is exactly what `check-refund.ts` exists to prove and it is not the same as proving a payment lands. | `spike/ndebit.ts` `payDebit`, the `body.res === 'ok'` branch | Unknown. The branch is four lines and reads the same `{"res":"ok","preimage":…}` shape `clink-debits.md:178-217` specifies and that `sales-report.ts`-adjacent code already handles, but nobody has seen it. | It moves real money and the slice brief says to ask before that. | One 10-sat debit of an invoice the account issues to itself, or the real oversell test above. Both need a decision that is not the implementer's to make. |

---

## Added by slice 8 (2026-08-21)

Two rows, both small, both verified to reproduce. Slice 8 closed more than it opened — the
`render.ts` row and half the `pending` row — so this section is short on purpose.

| what breaks | where | what it costs a user | why it is deferred | what fixing it looks like |
|---|---|---|---|---|
| **The refund journal is still the only record that a refund happened, and the startup reconcile was not built.** Slice 7's row named a buildable mitigation — reconcile against the node's outgoing payments at startup — and slice 8 built the *reporting* half of it (`matchingPayments`, `sales-report.ts --outgoing`, the pending-row print) and not the reconciling half. So the machinery to answer "has this already been paid?" now exists and nothing calls it at startup. | `spike/watch-sales.ts`, the block that loads the journal | Unchanged from slice 7: losing `spike/.refunds.json` can pay a refund twice, bounded by the node's frequency cap. | Doing it at startup means deciding, automatically, that a payment matched on **amount and time alone** is the refund in question — and the node stores no link back to the sale, so two refunds of the same amount in the window are indistinguishable. A heuristic is fine as evidence for a human and is a different thing as an input to whether money moves again. That is a decision about money, and the reporting half makes the manual version cheap enough that it was not worth forcing. | Read the node's outgoing payments once at startup, and for any journal row that is `pending`, print a **prompt** rather than a conclusion: "the node has one 1,000-sat payment 40 seconds after this row was written — mark it paid? [y/N]". The `--reconcile` mode slice 7's row already sketched. Everything it needs is now written and tested. |
| **`noBuyReason` cannot tell a missing offer from a mismatched one, and neither can the buyer.** An item priced in sats above the floor with no usable `clink_offer` gets one sentence — "Not payable on this page" — whether the listing carries no offer tag at all or carries one whose TLV 4 price disagrees with its `price` tag. `listing.ts` `buyableOffer` collapses both to `undefined` before `render.ts` sees anything. | `storefront/src/render.ts` `noBuyReason`, against `storefront/src/listing.ts` `buyableOffer` | Nothing, for a buyer — the next step is the same either way, which is to ask the seller. It costs the **seller**, who may not know their listing and their offer disagree, and whose page silently stops selling one item. | Telling them apart in the page means `buyableOffer` returning a reason rather than `undefined`, which widens the trust boundary's return type for a distinction only a seller can act on — and the seller-facing tool already exists and runs where the key is. | Either leave it and rely on `node spike/check-admin.ts`, which does have both events and can say which; or have `buyableOffer` return `{offer}` / `{reason}` and let the admin panel surface it. The buyer's copy should not change either way. |


---

## Added by slice 9 (2026-08-21)

Slice 9 closed more than it opened — the builder stopped publishing our neighbourhood into other
people's listings, and a `g` tag that had been wrong for eight slices got read for the first
time. What is left is three rows, and **the third is not a defect so much as the bill for
everything above it.**

| what breaks | where | what it costs a user | why it is deferred | what fixing it looks like |
|---|---|---|---|---|
| **CLOSED 2026-08-26 by item 13's last bullet, and it took exactly the fix this row's last column named.** `builder/src/admin.ts` `droppedMembers` compares the list about to be published against the `itemRefs` of the kind 30405 read off the relays on the last load, names every member that would be un-listed, and refuses to publish until the seller ticks a box. It is a **set difference rather than a length comparison**, which is one step past what this row asked for: "if the new member list is shorter" would wave through a same-length swap that still un-lists a real item. It sits **after** the quorum gate, so a short read (this row's original cause) still gets "Reload my items" rather than a confirmation prompt — asking someone to confirm un-listing items they never chose to drop is how you train them to tick without reading. The confirmation is asserted rendering in `builder/smoke.test.ts`. This row's fourth column called the collision with a legitimate delete the reason not to build it; the answer is that a guard which cannot tell a mistake from an intention must **ask** rather than decide, and that is what now unblocks M3's delete. Original text: **Publishing the sale re-lists only the items the panel last managed to read.** A kind 30405 is a replacement, so `publishSale` has to send every member every time — and the member list it is handed is `owned`, which is whatever `loadItems` got back from four relays on the last press of "Load my items". One slow relay, or a `querySync` that returns early, and pressing "Publish my sale" quietly drops the items that did not come back. `publishSale` verifies that the parser reads back as many members as it was **handed**, which catches an encoding fault and cannot catch a short read. | `builder/src/publish.ts` `publishSale`, against `builder/src/main.ts` `doPublishSale` | Items fall out of the collection. They are **not deleted** — they survive on the relays and `storefront/src/listing.ts` `orderBySale` renders collection members first and strays after, so they drop to the foot of the sale page in `d` order. A demotion, not a loss, and invisible unless the seller counts. | The honest fix needs a number to compare against, and nothing on a relay records how many items a sale is *supposed* to have. The count is in the collection currently on the relays — which is the thing being replaced — so comparing against it means "refuse to shrink the sale", and shrinking the sale is also what removing an item legitimately looks like. Guessing wrong in that direction blocks a real edit. | Read the current kind 30405 immediately before signing (one relay read, no signature) and, if the new member list is **shorter**, name the items being dropped and ask. The panel already prints its own item count next to the sale name, so a seller who looks can see the discrepancy today — that is the mitigation, not the fix. |
| **A new item does not appear in the sale's member list until the seller publishes the sale again.** Deliberate — `builder/src/listing.ts` says so and it saves a signature per item — but it is now a thing a seller has to know, where before slice 9 the collection was never published from the builder at all and the question could not arise. | `builder/src/listing.ts` `listingTags`, the `a` tag comment | A newly authored item renders at the **foot** of the storefront, after every collection member, rather than where the seller expects. Nothing is missing and nothing is unbuyable. | Re-signing the collection on every item publish costs one extra approval per item, on a budget (§5) that already had to grow a `1 + units` term. For a seller adding nine things in a row that is nine signatures to fix an ordering nobody has looked at yet. | Either publish the sale automatically after the *last* item rather than each one — which needs a notion of "last" that a form does not have — or say it in the publish result copy, which is one sentence and is the cheaper honest option. Not done: the copy in `#result-text` is already three sentences long. |
| **Every pixel of slice 9 is unrendered, and slice 9 is the most visual slice in the project.** The sticker sheet has never been printed, the `@media print` block that hides `<main>` has never run, `missingItemNote` has never painted, and the `geo:` link has never been tapped. `render.ts` and `sale.ts` decisions are tested — 58 storefront, 58 builder — and **the markup is not, by the same boundary slice 8 wrote down.** | `builder/src/stickers.ts`, `builder/src/style.css` `@media print`, `storefront/src/render.ts` `renderIndex`'s missing branch | Unknown, which is the point. A print stylesheet that has never been previewed is the class of thing that is either perfect or comically broken with nothing in between. | It is not deferred so much as **assigned**: `/docs/prompts/browser-verify-and-deploy.md` is a session that already exists and now covers six slices of unrun DOM. It was deliberately not folded into this one. | Run that session. Its list should now include: print the storefront (is the grayscale approximation good enough on paper — design.md §2.2 asks and only a person can answer); build and print the sticker sheet; visit `#/item/does-not-exist`; and tap the masthead location on a phone. |

## Added by the review panel (2026-08-21)

Not a slice. A five-lens review panel over `git diff main...HEAD` (money path, credentials,
protocol-vs-docs, hostile input, correctness), each finding then handed to a skeptic told to
refute it and to default to refuted when unsure. **15 raw findings, 8 verified, 6 survived, and
three of those six are the same bug** — the money-path, correctness and idempotency lenses each
found the watcher race independently, from different lines. Collapsed to four rows below. Line
numbers re-checked against the tree by hand on 2026-08-21 after the panel returned.

| what breaks | where | what it costs a user | why it is deferred | what fixing it looks like |
|---|---|---|---|---|
| ~~**The refund watcher can pay the same buyer twice, and corrupts the journal on the way.**~~ **CLOSED 2026-08-24 by milestone A (`e070ca4`), both halves, exactly as the fix column prescribed.** `spike/refund.ts` gained `inFlightGuard`, which DROPS an overlapping tick rather than queueing it — queueing would build a backlog behind the slow refund that caused the overlap, and the poll recomputes everything from the node each time. And `recordRefund`, which drops the write when the row is already `paid`. The monotonic rule lives on the JOURNAL rather than at the call site, so item 9's startup reconcile inherits it: every reader and writer of a row's `state` was grepped before landing it, and nothing in the tree moves a row out of `paid`. The five-minute `summarise()` timer is deliberately NOT guarded and the file says why — it is a pure reader, and guarding it would swallow the reminder of money still owed on exactly the run where the node answers slowly. The test is the deliverable as much as the guard: `--once` installs no timer, so nothing can drive the interval that raced, and the regression test drives the shape underneath it — read the journal, wait, write, pay — twice with overlapping awaits, with a second test asserting the same stub double-pays once the guard is removed. Original text: **The refund watcher can pay the same buyer twice, and corrupts the journal on the way.** `setInterval(… tick(), POLL_MS)` has no in-flight guard, and a refunding tick routinely outlasts `POLL_MS = 5_000` by design — `resolvePointer` alone is two sequential LNURL fetches at `FETCH_TIMEOUT_MS = 10_000` each. The journal row that makes the refund idempotent is not written until line 320, *after* that network call, so tick B re-reads the journal mid-flight, sees nothing at `settledByUs` (274) or `previous` (294), and resolves a second BOLT11 for the same settled invoice. Both call `payDebit` with the identical `k1For(row.invoice)`; the node's debouncer refuses the loser with GFY 1, and the loser's `record()` — an unconditional `journal[row.invoice] = {…}` with no state machine — writes `failed` over the winner's `paid`. `settledByUs` treats `failed` as retryable and `RETRY_AFTER_S` is 6 minutes, chosen deliberately to outlast the node's 5-minute k1 TTL, so the retry gets a fresh k1 the debouncer has already swept and pays for real. **The journal is the only durable double-refund guard (`refund.ts:66-92` says so outright) and this both bypasses and corrupts it.** | `spike/watch-sales.ts:653` (the timer), `:307` (the unguarded await), `:353` (`record`) | Real sats, twice, to a buyer owed them once. Bounded by the node's own `assertDebitFrequency` and by `--refunds` being opt-in, but not by anything in this codebase. | Raised 2026-08-21 by the panel; the user chose to batch the fixes rather than land them mid-review. Nothing about it is unclear — it is queued, not open. | Two lines. `let ticking = false` around the interval callback closes the race; `if (journal[row.invoice]?.state === 'paid') return` at the top of `record` makes the journal monotonic so a late refusal can never downgrade a settled row. Both are worth landing even though either alone would break the chain — the second is the one that survives someone later adding a concurrent caller. |
| ~~**`mintOffer` dedupes on label alone, so a price change makes every later retry mint again.**~~ **CLOSED 2026-08-24 by milestone A (`90dc844`), exactly as the fix column prescribed.** `matchingOffer` dedupes on the PAIR — `label === label && decodeNoffer(o.noffer)?.priceSats === priceSats` — and the existing TLV-4 re-derivation stayed where it was. It is a pure exported function so the rule is testable without a node, which matters more than usual here: testing it against the fixture account would prove nothing either way, because those five offers were minted natively and Manage `list` cannot see natively-minted offers at all (§13.20). An empty list there is correct behaviour, not a broken fix. Original text: **`mintOffer` dedupes on label alone, so a price change makes every later retry mint again.** `existing?.find(o => o.label === label)` picks the *first* same-label offer and price-checks only that one. A price edit deliberately leaves the superseded offer on the node (the header at `manage.ts:155-157` says so), so from then on the list holds two offers sharing a label: the old-price A and the new-price B. A retry after any post-mint failure finds A, sees the price disagree, falls through, and mints C. | `builder/src/manage.ts:170` | Unbounded inert offers on the seller's node after any price edit followed by a failed publish. Nothing is mispriced — the storefront reads the noffer — but this is the exact non-idempotent retry that slice 7's Phase 0 was supposed to have closed. | Same batch as above. Costs clutter, not money. | `find` over `label === label && decodeNoffer(o.noffer)?.priceSats === priceSats` — dedupe on the pair, not the label, and let the existing TLV-4 re-derivation stay where it is. |
| ~~**"Publish my sale" is live before the panel knows which sale it is editing.**~~ **CLOSED 2026-08-24 by milestone A (`90dc844`), and it took the LARGER of the two fixes this column offered plus the smaller one.** `noPublishSaleReason()` in `builder/src/admin.ts` is consulted both at the enable site and inside `doPublishSale`, so the guard protects every caller rather than one path — including the next entry point somebody adds. `loadPanel` sets `panelLoaded`, and clears it again if the read throws, because a failed read must not leave the button live on whatever the last good load said. **Item 13's quorum and show-the-count bullets came forward from milestone D with it** (`roadmap-review-findings.md` §13): it is the same button, and fixing only this half left it able to publish a member list that was SHORT rather than empty. `loadItems` now reports which relays actually contributed (nostr-tools' own `trackRelays`/`seenOn`) and publishing is refused below a majority; `#sale-cost` says how many items are about to replace the collection and how many relays it was read from. Item 13's refuse-to-shrink bullet deliberately stayed in D. Original text: **"Publish my sale" is live before the panel knows which sale it is editing.** `showSigner()` enables `#publish-sale` synchronously at `main.ts:116`, then fires `void loadPanel()` — a four-relay `querySync`. Until that resolves, `sale` is `draftFromSale(undefined)` (`d: 'sale'`) and `owned` is `[]`. A click in that window signs `saleTemplate(draft, pubkey, [], now)`: NIP-01 replaces on `(kind, pubkey, d)`, so it does not replace `yardsale-2026-08`, it publishes a *second* kind 30405 with zero member tags. | `builder/src/main.ts:357`, enabled at `:116` | A returning seller who clicks fast splits their storefront in two and un-lists every item from the new collection. | Same batch. Needs a click inside a sub-second window, which is why it reads low — but the failure is a published event, not a retryable error. | Enable `#publish-sale` from `loadPanel`'s completion rather than from `showSigner`, or gate `doPublishSale` on a `panelLoaded` flag. The first is smaller; the second survives someone adding another entry point. |
| ~~**The kill switch can report success while the grant it was meant to revoke stays live.**~~ **CLOSED 2026-08-24 by milestone A (`d77400a`), and it under-scoped by one branch exactly as `roadmap-review-findings.md` §9 said.** The mint moved below `--show`, `--revoke` AND `--reset` — `--reset` called `ResetDebit` on the same improvised key, so guarding only `--revoke` would have left a second switch reporting success. Both branches now go through one `killSwitch()`, which REFUSES when there is no key rather than inventing one, sends the RPC, and then **re-reads `GetDebitAuthorizations`**: the success line is printed from the after-state and never from the call returning, and if the node still reports the key AUTHORIZED it throws and tells the seller to ban it from ShockWallet by hand. If the node reports no grant at all it prints no success line, names any OTHER grants on the account, and exits 1. Two things the fix column did not have: `--show` now needs no key file (on a fresh clone it lists what the node actually has instead of inventing a key to compare against), and `--npub <hex>` kills a grant from a machine that does not hold the key — because the objection this WILL attract, that the switch now needs the node, is answered by the switch already needing the node. `BanDebit` is an RPC; requiring the node AND a local file is strictly worse than requiring the node. `check-refund.ts` drives `authorize-refunds.ts --revoke` rather than calling `BanDebit` directly, asserts no AUTHORIZED grant survives, and a new section 4b hides the key and asserts both branches exit non-zero, refuse, and mint nothing. Original text: **The kill switch can report success while the grant it was meant to revoke stays live.** The refund key is minted at module top level (119-122) *before* `--revoke` is handled (191). Run the kill switch on a machine where `spike/.refund-key` is absent — a fresh clone, a restored `.dev-key`, a deleted file — and it writes a brand-new random key, derives `refundPub` from that, and calls `BanDebit` on a pubkey that was never granted anything. Per the file's own note on `debitStorage.ts:43-47` that just creates a fresh banned row. It prints `# BANNED the refund key` and exits 0. | `spike/authorize-refunds.ts:119`, revoke at `:191` | The seller believes refunds are stopped. They are not. A watcher elsewhere still holds the original key and its grant. | Same batch. **This is the one to fix first regardless of severity ranking** — a kill switch that lies is worse than no kill switch, because it ends the seller's response instead of starting it. | Move the mint below the `--revoke` branch, and make `--revoke` fail loudly when `.refund-key` does not exist rather than inventing one. `/CLAUDE.md`'s "the refund path needs a hard cap and a kill switch" is only satisfied by a switch that can tell you it did nothing. |

**Refuted, and worth recording so they are not re-found:** the panel's claim that *every* non-zero
GFY is journalled as `failed` and can therefore double-pay was refuted on the grounds that every
payout journals before it spends, so the precondition is unreachable from the watcher's own flow —
the race row above is the reachable version of the same worry. A preimage-logging claim against
`spike/check-buy.ts:128-142` was refuted as out of scope: the block landed in `1afbd8d`, before
this branch.

**Verified by nobody — ~~seven findings the panel produced and the budget cut before the skeptic
stage~~. EMPTY AS OF 2026-08-24.** All seven were triaged by milestone A's item 5. Four are closed
below, three are open entries in the milestone-A section that follows, and none of them was left in
limbo because it was inconvenient. The scoreboard:

| claim | verdict | where it went |
|---|---|---|
| `render.ts:521` — the Buy form awaits `requestInvoice` with no `try` | **CONFIRMED**, and the trigger is narrower than "a rejection" | fixed, `63eb718` |
| `refund.ts:257` — `MAX_BODY_BYTES` is checked after the body is buffered | **CONFIRMED**, reproduced: 10 MB in 34 ms | fixed, `534a0ff` |
| `refund.ts:221` — no private-address check on either LNURL hop | **CONFIRMED**, reproduced against a loopback listener | fixed, `534a0ff` |
| `builder/index.html:206` — promises refund pointers the tool never emits | **CONFIRMED** | fixed, `63eb718` |
| `admin.ts:82` — a photo's dimensions treated as mandatory | **CONFIRMED**, and this repo already held the citation | open row below |
| `main.ts:507` — `loadPanel` overwrites typed sale-form input | **CONFIRMED from the code path**; unverified in a browser | open row below |
| `watch-sales.ts:468` — the `k1`/TLV-3 claim | **CONFIRMED at `:335`, and the roadmap's own reading of it was wrong** | open row below |

**The `k1` one is worth reading twice, because the instinct to check the citation was right and
the conclusion drawn from it was not.** `README.md` said to judge the claim against
`spike/ndebit.ts:82-86`, "which already reads `clink-debits.md:167-171` as a MUST that binds *when
TLV 3 is present* — so a `k1` sent without one is not obviously forbidden." That is half the rule.
`/docs/clink-notes.md` §3.3, quoting `clink-debits.md:163-172`, has **two** bullets: *"Present in
TLV `3` ⇒ the wallet MUST set `k1`"* and *"**Absent ⇒ the wallet MUST NOT invent one.**"*
`ndebit.ts:82-86`'s own comment states both. Our `.ndebit` carries no TLV 3 —
`authorize-refunds.ts` says so in as many words — and `watch-sales.ts:510` sends
`k1: k1For(row.invoice)`. So we invent one, and the spec says not to. The claim stands; only its
line number was wrong. It is an open row below rather than a fix, for reasons that are about
design rather than effort.

---

## Added by milestone A (2026-08-24)

Milestone A closed the four confirmed panel rows above and triaged all seven claims. What follows
is what it opened, plus the three claims it confirmed and deliberately did not fix. Same rules as
every other section: verified to reproduce, with the reason it was left and the shape of the fix.

**Two of these are holes the fixes themselves left, and they are first**, because a guard that
reads as a guarantee and is not one is worse than no guard.

**The BIP-353 row below was found on 2026-08-24 by the review, running `resolvePointer` against a real
Lightning address for the first time — the thing this ledger had been recording as never done since slice 7.
It is a PRE-EXISTING defect rather than one milestone A introduced, and it took one free call to find.**
The lesson is cheaper than the row: the LNURL branch is unreachable by any offline test, because every
address a test server can bind is one `isPrivateAddress` correctly refuses.

| what breaks | where | what it costs a user | why it is deferred | what fixing it looks like |
|---|---|---|---|---|
| **The refund path's address vetting is a whitelist of ranges, not a proof.** Item 4 replaced `fetch` with `node:https` so the address that is vetted is the address connected to — there is no TOCTOU left, and that was the shape the roadmap's `dns.resolve()`-then-`fetch` bullet could not have. What remains is the range table: `isPrivateAddress` enumerates loopback, RFC1918, link-local, CGNAT, the documentation and benchmark blocks, multicast and reserved, IPv6 loopback/ULA/link-local/multicast, the IPv4-mapped forms and the NAT64 prefix. It does **not** enumerate 6to4 (`2002::/16`) or Teredo (`2001::/32`), both of which embed an IPv4 address that could be private. **NARROWED 2026-08-24 by the milestone A review, which found the row understated the hole:** the IPv6 branches keyed off `startsWith('::')` and a mask on the first group, so every *uncompressed* spelling of loopback failed OPEN — `0::1` and `0:0:0:0:0:ffff:10.0.0.1` were measured as ALLOWED. Not exotic tunnelling, just a second spelling, and it was unreachable only because `URL` and `dns.lookup` canonicalise upstream. Fixed by canonicalising through `new URL` before testing; 6to4 and Teredo remain the honest ceiling. | `spike/refund.ts` `isPrivateAddress` | A buyer whose `refund_pointer` names a host resolving to a 6to4 or Teredo address that tunnels to a private v4 one reaches an internal endpoint over https. Narrow: it needs the seller's network to route those, and the endpoint to speak https with a certificate the watcher trusts. | Enumerating half of a CIDR table is how you get a check that reads as a guarantee and is not one. Either it is a published table or it is a named list, and a named list with its gaps written down is the honest version at this size. | Replace the branches with a published special-purpose-address-registry table, or drop to a positive rule — refuse everything that is not a global unicast address — which inverts the failure direction and would need measuring against real wallet hosts first. |
| **The startup reconcile cannot ask a daemon anything, and says so instead of guessing.** Item 9's prompt needs a TTY. Under launchd there is none, so a `pending` row with a matching outgoing payment prints its evidence, prints that nobody can answer, and stays `pending`. | `spike/watch-sales.ts`, the reconcile block above the first `tick()` | Nothing that money turns on — a `pending` row is already never retried automatically and never dropped, and it is reprinted every tick and every five minutes. What it costs is that an unattended watcher can never *settle* one, so the row accumulates until somebody runs the watcher from a terminal. | The alternative was refusing to start, and it is worse: this process is also slice 3's watcher, so stopping it leaves items advertised as available after they sell — an oversell, which is a NEW loss rather than a guard against one. Guessing is the third option and it is the one the review reversed the roadmap over. | A `--reconcile` mode that does the prompting and exits, so the daemon stays a daemon and a human settles rows on their own schedule. Or a journal edited by hand, which is what the log line currently tells them to do. |
| **`authorize-refunds.ts` is hardcoded to `.dev-key`, so the second seller cannot arm refunds at all.** `watch-sales.ts` is multi-seller — `--key` derives `.merida-key.refund-key`, `.merida-key.ndebit` and `.merida-key.refunds.json` — and the script that creates those two files writes `.refund-key` and `.ndebit`, unsuffixed, on the default seller's account. There is no flag. | `spike/authorize-refunds.ts:92-94`, against `spike/watch-sales.ts:83-90` | `node watch-sales.ts --key .merida-key --refunds` throws at startup with "needs `.merida-key.refund-key`", and there is no way to produce one. So an oversell on the second seller's sale is logged and never refunded — which `/docs/status.md` records as a fact about that seller without saying it is unfixable. | Found while fixing item 2 rather than scoped into it; adding `--key` to a script that mints a spend credential is money-path work, and doing it inside a commit about the kill switch would have buried it. | `const KEY = arg('key', '.dev-key')` and the same `suffixed()` treatment `watch-sales.ts:84` already has, for `REFUND_KEY_FILE` and `OUT_FILE`. Item 24's shape, applied to the third script. |
| **HALF CLOSED 2026-08-26 by item 27's first bullet: the `queued` row now says the true reason. The buy side is still open and is a DECISION, not a fix — see the note under this table.** `resolvePointer` looks up `<name>.user._bitcoin-payment.<domain>` when the first LNURL hop fails PERMANENTLY, and if a TXT record there starts with `bitcoin:` the row reads *"that is a BIP-353 address and this refund path speaks LNURL-pay"* instead of naming DNS. Bounded as hostile input: its own resolver at 2 s / 1 try, an outer wall-clock race, 16 records, 4 KB, a single validated DNS label, and it returns a BOOLEAN so the offer is never logged. Proven live on 2026-08-26 against `matt@mattcorallo.com` (a published BIP-353 test vector) in 392 ms, and proven NOT to fire against `nobodyatall@coinos.io`, a real LNURL host. **Still true and still open:** a real Phoenix address cannot be tested from the second machine, because a made-up name on `phoenixwallet.me` is NXDOMAIN at the BIP-353 name too and correctly falls back to the DNS message; and **nothing pays a BOLT12 offer.** Original text: **A BIP-353 address is the same `user@domain` shape as an LNURL one, and we assume LNURL.** `LN_ADDRESS.test()` accepts `name@phoenixwallet.me`, `lnurlpUrl` builds `https://phoenixwallet.me/.well-known/lnurlp/…`, and there is no such endpoint: the domain has NS records and **no A or AAAA record at all**. What it has is a TXT record at `<name>.user._bitcoin-payment.<domain>` carrying a `bitcoin:?lno=…` BOLT12 offer. **Found 2026-08-24 by running the refund path against a real address**, which is the test the ledger had been asking for since slice 7. Phoenix is a mainstream wallet, so this is the common case for one popular wallet rather than an edge one. | `spike/refund.ts` `resolvePointer`, the address branch, against `storefront/src/offer.ts` `LN_ADDRESS`. Filed as **roadmap item 27** (milestone E), and item 6 now warns against using a Phoenix address for the first real refund | The buyer's pointer looks valid to `render.ts`'s `isPointer` gate, so the invoice settles and the pointer is stored on it **forever** — the node cannot fix it afterwards. At the oversell, resolution fails. Since the DNS fix below it is now a `queued` row a human can act on; before that fix it was `failed` and retried every six minutes until the sale ended, with no line telling the seller anybody was owed money. | Detecting the TXT record is cheap; **paying a BOLT12 offer is not a fix, it is a feature.** `payDebit` sends a BOLT11 to the node, and whether this Lightning.Pub can pay a BOLT12 offer at all is UNVERIFIED — that is a spike, not a patch, and it lands after item 6 has proved the ordinary path. Refusing accurately is the honest interim and it is not yet built. | Two halves, separable. **Now:** look up `<name>.user._bitcoin-payment.<domain>` TXT when the LNURL hop fails to resolve, and if it answers, say *"that is a BIP-353 address and this refund path speaks LNURL-pay"* rather than naming DNS — a `queued` row a seller can act on at the table. **Later:** decide whether to resolve the `lno=` offer and pay it, which needs the node's BOLT12 support established first. The buy-side question is separate and larger: whether `isPointer` should refuse a pointer the refund path cannot use, which would move the failure from after the sale to before it. |
| **`buyableOffer` hands the page an offer whose pubkey is not a point on the curve.** `decodeNoffer` checks TLV 0 is 32 bytes and nothing more, so a malformed `clink_offer` produces a Buy button that can never work. Since `63eb718` it fails as one named sentence rather than as a dead form, but the button is still there. | `storefront/src/listing.ts` `buyableOffer`, against `storefront/src/offer.ts` `decodeNoffer` | A buyer fills in the form, presses Buy, and is told the item's payment pointer is unusable and to tell the seller. One wasted interaction on an item that was never payable. | Validating a point costs a secp256k1 decompress per item in the storefront's COLD path, on a phone, against a 33 KB gzip budget currently at 32.11 — for a case that only arises from a listing this project's own tools cannot produce (`builder/src/listing.ts` and `spike/seed-listings.ts` both write the offer they just minted). The precedent cuts the other way (`render.test.ts` asserts a sub-floor price gets copy rather than a Buy button that always fails), which is why this is a row and not a silence. | Decompress the point in `decodeNoffer` and return `null`, which fixes every caller including `spike/refund.ts` resolving a buyer's noffer. Measure the bytes and the cold-path cost first; `check-admin.ts` is the seller-facing place that could report it for free today. |
| **We invent a `k1` where the spec says not to.** `/docs/clink-notes.md` §3.3, quoting `clink-debits.md:163-172`: *"Absent ⇒ the wallet MUST NOT invent one."* Our `.ndebit` is a static pointer with no TLV 3 — deliberately, because `clink-debits.md:30-31` says a session ndebit "MUST NOT be published as a user's primary `clink_debit`" — and `watch-sales.ts:510` sends a `k1` derived from the settled invoice anyway. **This is the panel's `:327` claim with its citation corrected**, and the roadmap's reading of it ("not obviously forbidden") was wrong. | `spike/watch-sales.ts:510`, against `spike/authorize-refunds.ts`'s TLV-3 note and `spike/ndebit.ts:82-86` | Nothing today. The node accepts it: `doNdebit` runs `DedupeK1` on whatever `k1` arrives regardless of the pointer's TLV 3 (§13.28), which is how `check-refund.ts` proved the replay behaviour in the first place. What it costs is a deviation on the money path, in a protocol we are meant to be demonstrating. | Removing the `k1` loses the crash-loop guard the design deliberately bought (spec §7.3, §13.28) and buys nothing back. Doing it *properly* means a per-refund SESSION ndebit carrying TLV 3, which the account owner would have to mint per refund — a design change, not a fix, and one that needs the spec's intent rather than its letter. | Ask upstream what a static pointer's payout is supposed to use for single-use protection, since the node clearly expects a `k1` there. File it beside items 21 and 22. If the answer is "nothing", drop `k1For` and lean on the journal alone. |
| **`blobFrom` treats a photo's dimensions as mandatory where NIP-58 and Gamma make them optional.** `!photo.w \|\| !photo.h` returns `null`, and `draftFrom` then drops that photo from the draft's blobs. If every photo on an item lacks dimensions, `blobs` is empty, `hero` is undefined, `servers` is empty — and saving the edit republishes the listing with no `image`, no `thumb` and no `imeta` at all. **This repo already held the citation the panel did not give**: `storefront/src/listing.ts:111-113` reads `58.md:31`, `58.md:34` and Gamma `spec.md:135`, says the third element is optional and that index 2 "may legitimately be an empty string", and `parsePhoto` correctly returns `{url}` with no dimensions. So the parser and the re-publisher disagree, in this repo, in writing. | `builder/src/admin.ts:86` `blobFrom`, against `storefront/src/listing.ts:111-119` | A photo silently deleted by pressing Save on an item that was authored somewhere else. | Not reachable from anything this project writes: `builder/src/listing.ts:139-141` and `spike/seed-listings.ts:202-205` both always emit `WxH`, so it needs a listing published by another NIP-99 client under this seller's key and this sale's `d` prefix. Fixing it changes what gets **signed and published** on the edit path, which is not a change to make on the strength of an unreachable case at the end of a milestone. | Make `w`/`h` optional on `Blob`, and have `imetaTag`/`listingTags` write `["image", url]` or `["image", url, ""]` — Gamma's own instruction is that an absent dimension "should be respected by using an empty string", which `listing.ts:112-113` already quotes. Then the round trip preserves what it read. |
| **`loadPanel` overwrites sale-form input the seller has typed.** It calls `showSale()` unconditionally, which writes `#sale-title`, `#sale-summary` and `#sale-geo` from the relay's copy — and `loadPanel(false)` runs after **every item publish**. So: type a new sale name, publish an item, and the name reverts with no message. **The file already knows this hazard and guards the other half of it**: the line below reads *"The notes come second and only on a full load: re-reading them mid-session would throw away edits the seller has typed and not yet saved."* Same function, same risk, one guarded and one not. | `builder/src/main.ts` `loadPanel`, the `showSale()` call | Retyping. Nothing published is lost or wrong — `doPublishSale` reads the form, so what it would publish after the revert is what is already on the relays, which is a no-op rather than a wrong sale. | Item 5 is triage; only the `render.ts:521` claim was marked fix-on-sight, and this one costs a seller keystrokes rather than work that reached a relay. **The user-visible half is UNVERIFIED**: confirmed from the code path, not from a browser, because `loadPanel` returns early without a signer and a signer needs the phone. It belongs to `/docs/prompts/browser-verify-and-deploy.md`. | The `withNotes` flag one line below is the pattern: skip `showSale()` when the sale form is dirty, or when this is the post-publish reload rather than a full one. Two lines, and the browser session should confirm the symptom first. |


---

## Added by item 8 (2026-08-25)

One row, and it is the first defect in this ledger that was found by a test rather than by
reading. It is also the first that **cannot be fixed on the machine that found it**: the fix is
a republish, and republishing is signing.

| what breaks | where | what it costs a user | why it is deferred | what fixing it looks like |
|---|---|---|---|---|
| **The `geo:` link on the LIVE sale points 5.9 km away from the sale.** Slice 9 found the fixture's geohash had been wrong since slice 1 and corrected `spike/fixture.ts` `g` from `9ewmr4z` to `9ewmxg9`, **and never republished.** The kind 30405 on all four public relays still carries `9ewmr4z`, `created_at` 2026-08-21T02:30:59Z, which decodes to 20.6261, -103.3930 against Colonia Americana's 20.6742, -103.3683. Verified 2026-08-25 by decoding the `g` tag off a live relay read and by rendering the page: `<a class="where-link" href="geo:20.6261,-103.3930">Colonia Americana, Guadalajara</a>`. The repo is right and the relays are wrong, which is the opposite of the failure slice 9's note describes and is why nobody caught it: `spike/fixture.ts` reads as fixed, and every test that reads the fixture agrees. | the kind 30405 `yardsale-2026-08` on the four relays, against `spike/fixture.ts:47` | A buyer who taps the neighbourhood link on the live storefront (the exact gesture roadmap item 7 exists to perform) is sent to a point 5.9 km southwest of the sale. On the day, that is a person who does not arrive. The `location` tag beside it still reads correctly, so the page is self-contradicting rather than merely wrong. | The fix is `node seed-listings.ts`, which signs with `.dev-key` and publishes to four public relays. Neither the key nor the authority to publish exists on the machine that found this (see `/docs/status.md`, "THE SECOND MACHINE"), and this session was scoped to nothing that signs. | `node seed-listings.ts` on the machine with the key, then re-read the `g` tag to confirm. **Note the trap:** that script also re-cuts `.ladder.json`, so it is not a casual run. Do it deliberately, with the watcher stopped, and check `check-admin.ts` §4 afterwards. Cheaper alternative if a full re-seed is unwanted: republish the kind 30405 alone, since the geohash lives on the sale event and not on any 30402. |

**The general lesson, which is worth more than the row.** A corrected constant in the repo and a
stale event on a relay are indistinguishable to every test in this project, because all of them
read the repo. The relays are the deployment target and nothing verifies them against the source.
`check-deploy.ts` compares Blossom and the gateway against the **manifest**, not the listing
content against the fixture, so it passes on this defect, and did.

---

## Added by items 16 and 17 (2026-08-25)

One row. It is small, it is honest, and it is the same shape as the thing item 8 exists to
prevent, which is why it is written down rather than waved past.

| what breaks | where | what it costs a user | why it is deferred | what fixing it looks like |
|---|---|---|---|---|
| **Item 17's new price-disagreement sentence has never rendered.** `noBuyReason`'s `priceDisagrees` branch is covered by three tests in `render.test.ts` and by nothing in a browser. It cannot be reached from the smoke fixture: producing a mismatched item means a **signed** kind 30402 whose `price` tag and `clink_offer` TLV 4 disagree, `SimplePool` verifies every event it accepts, and minting one needs a private key this codebase must not hold (rule 2). Verified 2026-08-25 that no item on the live sale is in this state, so the branch is unreachable from real data too. | `storefront/src/render.ts` `noBuyReason`, the `item.priceDisagrees` branch | Nothing today, and that is the point of the row rather than a reason to skip it. If the string is malformed, or the `.buy-none` section renders it wrongly, the first person to find out is a buyer standing in front of a price that is wrong. | The **wrapper** is proven: `renderBuy` builds the identical `.buy-none` section for the fiat and free branches, and `smoke.test.ts` renders both against live relay data. So what is unproven is one string in a branch whose structure is tested, which is a much smaller claim than "unrendered markup". Closing it properly needs a mismatched item on a real sale, and publishing one is signing. | On the machine with the key: publish a throwaway 30402 whose `price` tag disagrees with its `clink_offer`, load it, look at the sentence, then take it down. Roughly the cost of one `seed-listings.ts` run against a scratch `d`. Alternatively capture that event once into `smoke-fixture.json` and the branch becomes permanently smoke-tested, at the cost of a fixture item that is deliberately broken. |

**Worth keeping either way:** this is the first row in this ledger whose fix is cheap, whose cost
today is zero, and which exists only because the project decided that "tested" and "has rendered"
are different claims. Item 8 is what makes that distinction sayable.

---

## Added by the review of the dead-ends sweep (2026-08-27)

Two rows, both from reading the branch back before merging it. Four other findings from the same
pass were fixed rather than filed: a blank fiat price republishing as `["price","0","MXN"]`, a
fractional fiat price being editable and then unpublishable, two docblocks that contradicted the
code under them, and `render.ts` keeping two literal copies of the currency predicate M3 exported
to have one of. These two are the ones that are decisions rather than defects.

| what breaks | where | what it costs a user | why it is deferred | what fixing it looks like |
|---|---|---|---|---|
| **Item 27's message never reaches a buyer whose name half has an underscore.** `bip353Name` requires the local part to be one bare LDH DNS label, so `bob_smith@phoenixwallet.me` returns null, the lookup never happens, and the `queued` row falls back to the old message that blames DNS. Underscores are common in real Lightning addresses. | `spike/refund.ts` `bip353Name` | A seller with a Phoenix buyer whose address has an underscore is sent to debug a hostname that was never wrong, which is exactly the dead end item 27 exists to close, for a subset of the addresses it was built for. Fail-safe: the fallback is the message that shipped before, never a wrong claim. | Whether BIP-353's "encoded as a DNS label" admits `_` is a spec question, not a plumbing one, and this repo does not guess at protocol details (/CLAUDE.md). The strictness is also what stops `a.b@host` reaching into a zone the address does not name, so widening it is a change to a bound on hostile input and deserves its own reading of the BIP. | Read BIP 353 on the user part, then either widen the label pattern to admit `_` (still one label, still no dots) with the citation in the comment, or write `UNVERIFIED` beside the current pattern and leave it. Both halves are already covered by `refund.test.ts`, so the change is one regex and one assertion. |
| **`builder/smoke.test.ts` reimplements `showFiat` rather than calling it.** The two fiat tests drive `page.evaluate` to set the same attributes `main.ts` `showFiat` sets, so they prove the browser behaviour they claim (a `disabled` control leaves constraint validation, a hidden `required` one does not) and prove nothing about whether `showFiat` still does that. | `builder/smoke.test.ts`, the M3 section | Nothing today. It is a test that would keep passing through a regression in the function it is named after, which is the failure mode item 8 exists to prevent, one level up. | `showFiat` is a closure over module state in `main.ts` and is not exported; reaching it from a test means either exporting it or driving the real edit path, and the real edit path needs a signer and a four-relay read. Neither is available on the machine that found this. | Cheapest honest version: drive the page's own edit flow in the smoke test with the relay read stubbed the way `storefront/smoke.test.ts` already stubs it, and assert the attributes afterwards rather than setting them. That makes it one test of `showFiat` instead of two tests of the markup. |

---

## Documentation drift

A different kind of debt, listed so the eventual restructure has a target. **Nothing here was
fixed** — this section is a map, not a changelog.

**The pattern under all of them:** corrections land in the prose sections and the summary tables
keep the superseded answer. Every item below is a table, a diagram or a checklist row that a
later prose paragraph already overrules. A restructure that only rewrites prose will leave every
one of these standing.

**Slice 6 fixed the three it touched** (marked ☑ below) and left the rest, per the brief: fix the
drift you actually walk through, do not open a documentation project inside a build slice.

- **"One 24242 auth for the whole batch"** survives in three places, all contradicted by
  `spec.md:266` ("Dead — measured false in slice 1"), findings §9 (`spike-findings.md:809`:
  "one signed kind 24242 auth per blob. Do not batch.") and findings §13.11:
  - `docs/spec.md:508` — the §7.1 flow diagram
  - `docs/spec.md:1080` — the §11 question 9 row: "**Batching is permitted**"
  - `docs/spike-findings.md:1056` — the §12 corrections table: "one signature covers upload + mirroring"
- ☑ **React. FIXED 2026-08-21.** `spec.md` said both "no React, Tailwind or shadcn/ui" and, 116
  lines later, "the builder, which is React and can afford the SDK". Slice 6 owned the React
  decision, so it owned the drift: the second claim is struck and corrected, the §9 "revisit at
  slice 6" line is closed rather than deferred again, and `design.md`'s §5 heading and stack
  table now say what shipped. The answer is measured — the whole admin panel cost +4.3 KB gzip
  against React's ~45 KB floor.
- ☑ **Strict mode = delete the offer on depletion. FIXED 2026-08-21.** `spec.md` §11 q4 and
  `spike-findings.md` §12's §7.4 row both still said it. Slice 6 owned the "does mark sold delete
  the offer" decision, so it owned the drift: both rows now carry the decision that landed —
  nothing is done to the offer at all, the sold listing drops its `clink_offer` tag, and the
  invoice history stays readable for slice 7. Spec §7.4 itself is rewritten with the reasoning.
- ☑ **`docs/design.md` is a slice-0 document with patches. MOSTLY FIXED 2026-08-21 by slice 9**,
  which was obliged to touch it because §3 is the print spec slice 9 implements and §4 is the
  sticker spec. §5 was already corrected in slice 6. What slice 9 fixed and why it stopped being
  cosmetic:
  - **§2.2 and §3's "1-bit dithered thumbnails"** were a print *requirement* resting on a pipeline
    stage that was never built, in the slice that implements §3. Both now say there is no
    dithering, that what prints is `grayscale + contrast`, and — the part worth keeping — that
    the open question is not "when do we add it" but "is the approximation good enough on paper",
    which only a print preview answers. `style.css`'s two stale `ponytail:` comments pointing at
    slice 4 were corrected with it. **The instruction not to build the dithering to make the
    document true is now in the document.**
  - **§2.5's blurhash/thumbhash placeholder** now records the decision (with findings §13.21's
    citation) instead of describing a plan, and §2 as a whole marks which of its six items
    shipped, in which slice, and which two are deliberate absences.
  - **§4** carries the slice-9 decision about where the sticker sheet lives, and §1's masthead
    paragraph says what "editable in the admin panel" turned out to actually require.
  - Still standing: nothing in §2/§3/§4/§5. §6 ("theming: out of scope") is unexamined and was
    always meant to be.
- ☑ **The node account balance is both 6,000 and 8,000 sats. FIXED 2026-08-21, and measured
  rather than picked.** `node spike/sales-report.ts` reports **three settled invoices totalling
  8,000 sats** (`plants` 6,000, `mugs` 2×1,000) and `lncli listchannels` agrees on 8,000 local.
  The 6,000 figure was correct on the morning of 2026-08-21 and stale by that evening. All three
  `spec.md` sites now say 8,000, including §12 where it is the number slice 7's refund frequency
  cap gets set against — so that cap is now sized against a measured balance rather than a
  remembered one.

  **RE-MEASURED 2026-08-23, and this row is the pattern rather than the exception.** It is now
  **four settled invoices totalling 9,000 sats** (`plants` 6,000, `mugs` 3×1,000); the third
  `mugs` unit settled on the evening of 2026-08-21, hours after the figures above were written.
  All three `spec.md` sites and `docs/status.md`'s two rows updated. The consequence worth
  keeping: §12's caveat that "8,000/day equals the node's whole outbound balance, so the balance
  binds before the rule does" **is no longer true** — the account is at 9,000 against an 8,000/day
  cap, so the cap binds first and has become load-bearing without anybody deciding it should.
  A number measured once is a number that goes stale; this one is worth re-reading before any
  run that spends.
