{
  "name": "shots",
  "private": true,
  "type": "module",
  "description": "Screenshot capture for the demo PDF. Not part of the product build — same throwaway status as /spike.",
  "scripts": {
    "capture": "node capture.ts"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
